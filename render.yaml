services:
  - type: web
    name: sap-pm-emulator
    env: node
    region: oregon
    plan: free
    buildCommand: npm install
    startCommand: node scripts/start.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
